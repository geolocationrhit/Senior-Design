"interfaces" goes in /etc/network
"startupScript.sh" goes in /etc/init.d/, and rc.local goes in /etc/
"75-persistent-net-generator.rules" goes in /lib/udev/rules.d/

Make sure to do a "make" in the repository to compile all of the helper functions for the GPS and compass
